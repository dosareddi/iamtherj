GiftForce

GitHub branching practice.
--------------------------
For each new changelist
git checkout -b <branch>
git fetch
git add ...
git commit ..
git push origin <branch>

Github starting afresh
----------------------
To associate to your heroku repo, heroku git:remote -a giftforce
