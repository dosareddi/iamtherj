GiftForce

This repo provides a way for clients to talk to workers using SMS. Workers will communicate using Slack, worker management is done 
using Slack (programmed) commands.

Commmitting and reloading instances
-----------------------------------
git commit -am . && git push origin master && git push heroku master


Github starting afresh
----------------------
To associate to your heroku repo, heroku git:remote -a giftforce
